#services:
#  web: #или nginx
#    image: nginx
#    ports:
#      - 83:80  # и тут
#    volumes:
#      - ./docker/nginx/nginx.conf:/etc/nginx/conf.d/default.conf
#      - ./:/var/www/html
#
#
#  php-fpm:
#    build: ./docker/php
#    volumes:
#      - ./:/var/www/html
#      - ./docker/php/Xdebug.ini:/usr/local/etc/php/conf.d/docker-php-xdebug.ini
#
#
#  postgres:
#    image: postgres:16
#    volumes:
#      - ./docker/postgres/data:/var/lib/postgresql/data
#    environment:
#      - POSTGRES_USER=USER # {$DB_USER}
#      - POSTGRES_PASSWORD=PASS #{$DB_PASSWORD}
#      - POSTGRES_DB=lardb #{$DB}
#    ports:
#      - 5435:5432 #переменные укажи!
#
#
          #проверь все пути!
services:
    web:
        image: nginx:1.28.0    #проверь версию
        container_name: nginx
        volumes:
            - ./docker/nginx/nginx.conf:/etc/nginx/conf.d/default.conf
            - .:/var/www/html
        ports:
            - "${PROJECT_PORT}:80"
        restart: always
        depends_on:
            - php-fpm

    php-fpm:
        build:
            context: .
            args:
                USER_ID: ${UID} #че?
            dockerfile: docker/php/Dockerfile
        container_name: php-fpm
        restart: always
        volumes:
            - ./docker/conf/php/php.ini:/usr/local/etc/php/conf.d/php.ini
            - .:/var/www/html
        depends_on:
            db:
                condition: service_healthy

    db:
        image: postgres:17-alpine  #у тебя 16 обнови до 17
        container_name: database
        restart: always
        environment:
            - POSTGRES_DB=${DB_DATABASE}
            - POSTGRES_USER=${DB_USERNAME}
            - POSTGRES_PASSWORD=${DB_PASSWORD}
        volumes:
            - ./docker/postgres/pgdata:/var/lib/postgresql/data
        ports:
            - "${DB_PORT}:5432"
        healthcheck:
            test: ["CMD-SHELL", "pg_isready -U ${DB_USERNAME} -d ${DB_DATABASE}"]
            interval: 20s
            timeout: 10s
            retries: 5
            start_period: 10s
